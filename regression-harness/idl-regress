#!/usr/bin/env bash
# Thin wrapper: runs the harness via scala-cli with the harness sources as input.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec scala-cli run "$SCRIPT_DIR" -- "$@"
