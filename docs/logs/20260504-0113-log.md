# Review-loop session log — 2026-05-04 01:13 (PR-03.1 planning, blocked on R1)

## Original user request

User invoked `/review-loop` with the brief from `./job.md`: implement PR-03.1 — pre-modernization Layer A goldens harness for the idealingua-v1 compiler. New sbt module `idealingua-v1-test-harness` cross-built Scala 2.13 + 3, four contractual sbt tasks, goldens for "all 22 test domains × 3 surviving languages (Scala, TypeScript, C#)", F2 TBLOB audit, freeze tag `wire-format-baseline-2026-05-03` (commit only — user cuts the tag manually post-merge).

## What this session covered

This was the **planning** phase of PR-03.1, the first PR of Milestone 2 (Implementation). Execution was blocked before T1 could start.

### Phase: Setup (orchestrator)

- Read `./tasks.md` and `./defects.md` end-to-end. M1 closed clean, M2 had no breakdown.
- Split `./defects.md` per `job.md` instructions: copied existing M1 audit trail to `./defects-m1.md` (closed, append-only); reset `./defects.md` to a fresh header for the implementation phase, with a back-reference to `defects-m1.md`.
- Enumerated the corpus structure under `./idealingua-v1/idealingua-v1-test-defs/src/main/resources/defs/main-tests/source/`:
  - `idltest/` — 22 `.domain` files
  - `izumi/test/` — 4 `.domain` files (clashing, domain01, domain02, domain03recursive01) plus `clashing/another.domain` in a subdirectory
  - `overlaytest/` — `withoverlay.domain` plus overlay companions

### Phase: Planning (subagent)

Spawned the Plan subagent (model `opus`) with a self-contained brief covering:
- Authoritative inputs to read (tasks.md, defects-m1.md PR-03 entries, PR-03 master plan focusing on §3/§5/§9/§11/§12, PR-02 master plan §1/§6, meta-plan wire-format inventory).
- Working-dir layout (the nested `./idealingua-v1/` subdirectory) and corpus structure.
- Locked decisions (C1–C12, L4–L6, Q3, F1–F4) — non-relitigable.
- Out-of-scope items (compiler logic changes, new typer code, Layer B/C, wire-format spec doc, Go/Protobuf goldens).
- Specific source-grounded investigation tasks: TypespaceCompilerBaseFacade entry point, IDLTestTools loading pattern, sbtgen.sc structure, sbt version, corpus loader signature, per-language emitter entry points, TBLOB usage audit, corpus count verification, streams.domain behavior, cross-build feasibility.
- Deliverables: a plan doc at `./docs/drafts/20260503-2300-PR0301-baseline-harness-impl-plan.md` plus a structured ledger update brief.

The planner returned a thorough, source-grounded plan inline (read-only constraint prevented writing the file). The orchestrator persisted the plan at the requested path. Key planner findings:

1. **Build-system entrypoint**: `./build.sbt` is autogenerated from `./sbtgen/Deps.scala` via `./sbtgen.sc` (a 9-line scala-cli wrapper). Executor must edit `Deps.scala` and regenerate; `build.sbt` direct-edits forbidden.
2. **Compiler integration**: harness wraps `TypespaceCompilerBaseFacade.compile(toCompile)` (`…/transpilers/.../TypespaceCompilerBaseFacade.scala:11`) to get an in-memory `Layouted(Seq[ExtendedModule])`, filters to `DomainModule` only (excludes `RuntimeModule`), and writes `Module.content` UTF-8 bytes to per-language golden directories.
3. **Determinism**: harness pins manifests with `izumiVersion = "test-harness"`, `version = "0.0.0"`, and PLAIN layouts to avoid `MacroParameters.*`-driven drift.
4. **Cross-build feasibility**: confirmed — entire transitive dep chain (test-defs, runtime-rpc-scala, transpilers, core, model, compiler) cross-builds 2.13 + 3.8.3 already.
5. **Corpus contradiction (R1)**: master plan §6 audit (rows 17, 22) implies broader 28-file corpus including `izumi/test/` and `overlaytest/`; master plan §12 PR-03.1 narrows to `idltest/` (22 files); job.md contains the same contradiction within itself ("22 test domains" + "every `.domain` file under main-tests/source/"). Planner recommends broader; orchestrator escalates.
6. **Goldens layout**: outside `src/main/` (off the classpath) at `./idealingua-v1/idealingua-v1-test-defs/golden/`. Master plan §3 line 191's `src/main/resources/golden/` location would inadvertently put goldens on the test-defs classpath; orchestrator brief's location is preferred.
7. **TBLOB audit (F2)**: `command grep -rn 'blb\b\|TBLOB' …/defs/main-tests/source/` returns zero matches. F2 vacuous on the in-scope corpus.
8. **TBLOB-emitter divergence (F5, surfaced)**: ALL THREE transpilers diverge from locked Q3. Scala maps TBLOB → `Array[Byte]` (Circe encodes as JSON array of integers, not base64); TS and C# emit `???` (NotImplementedError on transpiler output). Out of scope for PR-03.1 (corpus has zero TBLOB) but urgent for any PR-03.3 fixture authoring that includes TBLOB. Captured as new follow-up F5 in `tasks.md`.
9. **Streams handling**: `streams.domain` compiles cleanly; the Scala/TS/C# translators silently drop `domain.streams`, emitting only the `Nuthing` DTO from the file. Matches locked C5 (deprecated-but-supported).

### Phase: Execution

**Not started.** Blocked on R1 (corpus scope).

## Final ledger state

- `./tasks.md`:
  - M2 status: `[~]` (in progress, planning complete).
  - New "Milestone 2 — PR breakdown" section with PR-03.1 marked `[!]` blocked on R1; PR-03.2 through PR-03.6 and PR-02 IMPL-1..N as `[ ]`.
  - New cross-cutting note **C13 / R1** marked `[!]` (corpus-scope decision pending).
  - F2 status updated with planner's vacuous-grep finding (still `[ ]`; flips to `[x]` once executor reconfirms post-T4).
  - New follow-up F5 (TBLOB Q3 divergence) added as `[ ]`.
- `./defects.md`: fresh M2 ledger, no entries yet (no execution → no defects).
- `./defects-m1.md`: copy of the closed M1 audit trail.
- `./docs/drafts/20260503-2300-PR0301-baseline-harness-impl-plan.md`: full PR-03.1 plan doc (§1–§12, ~12 risks/open questions documented).
- `./docs/logs/20260504-0113-log.md`: this log.

## Termination reason

`reason = blocked` (per skill I6). The corpus-scope contradiction between job.md, master plan §6, and master plan §12 PR-03.1 is a real ambiguity that requires user judgment. Cutting a baseline against the wrong scope and then committing the freeze tag `wire-format-baseline-2026-05-03` (locked, immutable per L6) would be irreversible — narrower-then-broader requires a follow-up baseline PR; broader-then-narrower is also problematic but easier to recover.

## Question for the user

**R1 — Corpus scope for PR-03.1.** Three possible answers:

1. **Broader (28 files)** — recommended by planner. Includes `idltest/` (22) + `izumi/test/` (5) + `overlaytest/` (1). Matches master plan §6 audit row 17 (cross-domain refs to phase, clones, aliases2) and row 22 (izumi/* refs). Risk: `izumi/test/clashing.domain` + `izumi/test/clashing/another.domain` may declare the same domain ID and trip `DuplicateDomainsRule` — discoverable in execution, recoverable by excluding one if so.

2. **Narrower (22 files)** — matches the job.md "22 test domains" phrasing and master plan §12 PR-03.1 description. Conservative; lower risk; the freeze tag captures only the `idltest/` baseline. If PR-03.2 or later requires the additional fixtures, a PR-03.1.5 baseline-extension PR would be cut against an extended freeze tag (e.g., `wire-format-baseline-2026-05-03-ext`).

3. **Other** — e.g., narrow further (drop streams.domain) or broaden further (include `defs/overlays/`, `defs/overlay-version/`, `defs/subdir/` sibling roots).

The orchestrator's read: option 1 (broader) preserves the master plan §6 audit's coverage assumptions; option 2 preserves job.md's explicit "22" count and minimizes the duplicate-domain risk. There is no objectively correct choice without a user decision on what exactly the freeze tag is supposed to baseline.

Once R1 is resolved, execution resumes from T1 (scaffold module). All other planner-flagged risks (R2 duplicate-domain, R3 manifest determinism, R4 concurrent PR-02 IMPL-8, R5 partial language regression, R7 sbtgen toolchain, R8 Layer A diff policy enforcement) are tractable inside the inner loop without further user input.

## Pending informational items (no decision required to unblock R1)

- **F5** — TBLOB-emitter divergence from Q3. The user originally locked Q3 as "base64 for all three languages" with the expectation that PR-02 would update emitters to match. Planner's audit of current emitters confirms the gap is wider than PR-03-D02's C#-only finding. Tracked in `tasks.md` for the user's awareness; will be surfaced again the moment any TBLOB-using fixture is proposed in a future PR.
