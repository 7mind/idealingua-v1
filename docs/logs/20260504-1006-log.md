# Review-loop session log — 2026-05-04 10:06 (PR-03.1 shipped)

## Original user request

`/review-loop` with the brief from `./job.md`: implement PR-03.1 — pre-modernization Layer A goldens harness for the idealingua-v1 compiler. The user later answered two blockers raised during the loop: R1 (corpus scope = broader 28 files) and F6 (keep the `ScalaTranslator` determinism fix despite the "purely additive" rule in job.md).

## What shipped

Single commit `1b8e1e2` on `wip/necromancy`: **PR-03.1: pre-modernization Layer A goldens harness**.

- New sbt module `idealingua-v1-test-harness` (cross-built Scala 2.13.18 + 3.8.3) under `idealingua-v1/idealingua-v1-test-harness/` with 6 source files: `HarnessCorpus`, `HarnessOptions`, `GoldenCompile`, `GoldenGenerator`, `GoldenVerifier`, `HarnessMain`.
- Four contractual sbt tasks wired: `regenerateGoldens` and `verifyGoldens` functional; `runWireFixtures` and `runCrossLangInterop` no-op placeholders.
- 700 Layer A goldens checked in under `idealingua-v1-test-defs/golden/{scala,typescript,csharp}/` (214 / 257 / 229) covering all 28 `.domain` files under `main-tests/source/`.
- 13-line determinism fix in `ScalaTranslator.scala` (user-approved) — replaces `.toMultimap.view.mapValues(_.flatten.toSeq)` with `.groupBy(_._1).toSeq.sortBy(_._1.toString).map { ... }`.

Verification: `sbt verifyGoldens` exits 0 against the committed goldens; cross-JVM determinism verified via two cold sbt-batch regen + sha256 set match across 700 files; smoke test confirms a corrupted golden surfaces a clean `[error] (... / verifyGoldens) ... Golden mismatch: ...` line and exits 1 without killing the sbt daemon.

## Loop iterations

### Outer loop O1 — plan seeding (Plan subagent, opus)

Spawned the planner with a self-contained brief covering authoritative inputs (tasks.md, defects-m1.md PR-03 entries, PR-03 master plan §3/§5/§9/§11/§12, PR-02 plan §1/§6, meta-plan wire-format inventory), the working-dir layout (the nested `./idealingua-v1/` subdirectory), the four locked decisions (C1-C13, L4, L6, Q3, F1-F4), explicit out-of-scope items, and 10 specific source-grounded investigation tasks. Planner returned `docs/drafts/20260503-2300-PR0301-baseline-harness-impl-plan.md` (12 sections, source-grounded with file:line citations) plus a structured ledger update brief. Planner surfaced two open issues:

1. **R1 corpus scope contradiction** between master plan §6 (broader 28-file: `idltest/` + `izumi/test/` + `overlaytest/`), §12 PR-03.1 (narrower `idltest/*.domain` only, 22 files), and `job.md` ("22 test domains" + "every `.domain` file under main-tests/source/").
2. **F5 TBLOB-emitter divergence** — found that all three transpilers diverge from the locked Q3 (Scala emits Circe int-array, not base64; TS and C# emit `???`). Out of PR-03.1 scope (corpus has zero TBLOB) but recorded as follow-up F5.

R1 escalated as session blocker → committed planning ledger state at `b4556f9`, returned to user.

### User input — first round (R1 resolution)

User: "ok, go broader, fix docs". Resolved R1 = broader 28 files. Updated `tasks.md` (C13 `[x]` resolved; PR-03.1 `[!]` → `[~]`); updated PR-03.1 plan doc §5 / §11 R1 to record the resolution; surgical edits to PR-03 master plan §11 Q9 (`[x]` resolved) and §12 PR-03.1 wording (corpus path → `main-tests/source/**/*.domain`; build entrypoint corrected from `build.sbt:929` direct-edit to `sbtgen/Deps.scala`). Committed at `bcd0682`.

### Inner loop iter 1 — T1 scaffold (executor → reviewer → fix)

**Executor** (general-purpose, sonnet) ran T1 — added `testHarness` ArtifactId + Artifact block to `sbtgen/Deps.scala`, added 4 `taskKey` declarations to root `imports`, wired no-op stub bodies, regenerated `build.sbt`, created `Placeholder.scala`. Cross-build verified on Scala 2.13.18 and 3.8.3; all four tasks invocable.

**Surprise**: executor's first regen used `./sbtgen.sc` without `--js` flag, producing a 482-line build.sbt diff that dropped the four Scala.js plugin declarations from `plugins.sbt` and the JS modules from `build.sbt`. Orchestrator caught it via `git diff --stat` review, identified `--js` flag from `./sbtgen.sc --help`, re-ran with the flag → diff shrank to 160 lines (harness-only, plugins.sbt unchanged). Recorded as a project convention in the PR-03.1 Completed entry.

**Reviewer** (general-purpose, opus) raised 8 findings, all nit/minor:
- D01 (nit): runWireFixtures/runCrossLangInterop placeholder messages drifted from plan §3 wording.
- D02 (minor, deferred): regenerateGoldens/verifyGoldens stubs exit 0 silently — a footgun for T2/T3, but T1 plan committed to exit-0.
- D03 (nit, no action): dead `Test/*` config in a no-test module — sbtgen template artifact.
- D04 (nit, no action): `refreshFlakeTask` whitespace realignment — deterministic side-effect of the new Import block.
- D05 (minor): `private[harness] object Placeholder` may trigger CI `-Wunused:all` on Scala 3.
- D06 (minor, forward-looking T2/T3): `.raw` Scala strings in `Deps.scala` for task bodies are brittle.
- D07 (nit, forward-looking T2/T3): `println` instead of `streams.value.log`.
- D08 (nit, no action): `pattern var charIn` silencer — fastparse-specific dead config from sbtgen template.

**Fix subagent** (general-purpose, sonnet) applied D01 + D05: aligned the two runWire*/runCross* placeholder strings to plan §3 wording; dropped `private[harness]` modifier from `Placeholder.scala`. Cross-build re-verified.

### Inner loop iter 2 — T2 + T3 implementation (executor → reviewer)

**Executor** (general-purpose, sonnet) implemented `regenerateGoldens` and `verifyGoldens`. Created 6 harness sources, wired real task bodies via `(Compile / runner).value.run(mainClass, fullClasspath, args, log)` against `RegenerateMain` / `VerifyMain` entry points (architectural deviation from plan §3 — the planner sketched direct method calls, but task bodies compile against the meta-build classpath which doesn't see the harness's compiled classes).

**Major surprise**: executor surfaced **F6** — legacy `ScalaTranslator.translate()` collected aliases via `.toMultimap` (immutable HashMap) and iterated to emit package-object.scala alias lines in JVM-instance-dependent order. Two cold sbt-batch regenerateGoldens runs produced different alias orderings on `idltest/aliases`, `idltest/aliases2`, `izumi/test/domain01..03`. Without a fix, Layer A's byte-strict contract is unenforceable. Executor applied a 13-line surgical fix (sort by `ModuleId.toString` after `groupBy`); 700-file sha256 audit across two cold sbt JVMs confirms determinism.

The fix is in `idealingua-v1-transpilers/.../toscala/ScalaTranslator.scala` — a compiler module. `job.md` explicitly forbids compiler changes ("Do not touch the typer or any compiler module. PR-03.1 is purely additive."). Orchestrator escalated to user: "Keep the fix? Revert? Land as PR-03.0.5 first?"

### User input — second round (F6 resolution)

User: "Keep the fix. Forget 'purely additive' - that's acceptable". Resolved F6 = keep the fix as a user-approved deviation from the original "purely additive" rule. Updated tasks.md (F6 `[x]` resolved) and defects.md (D09 resolved with the user's verbatim quote).

### Inner loop iter 3 — final review of the full PR-03.1 diff

**Reviewer** (general-purpose, opus) ran on the complete uncommitted state (harness sources + ScalaTranslator fix + 700 goldens + Deps.scala/build.sbt). Three findings:

- **D11 (major, fixed)**: `VerifyMain.main` called `sys.exit(1)` on verification failure. sbt 1.12 dropped `TrapExit` (JDK 17+ deprecates `SecurityManager`); `sys.exit(1)` calls `System.exit` directly, killing the sbt daemon in interactive mode. The earlier T6 smoke test passed because `sbt -batch` creates a fresh JVM per invocation, masking the bug.
- **D12 (minor, fixed)**: `GoldenGenerator.deleteRecursively` deleted entries during `Files.list().forEach()` traversal — implementation-defined per POSIX. Empirically benign on ext4 but brittle on tmpfs/NFS/Windows.
- **D13 (minor, deferred to F7)**: same `groupBy → iterate` pattern as F6 in `TypeScriptImports.render`, `TypeScriptImports.scala:212`, `CSharpImports.scala:112`, `ServiceRenderer.scala:65`, `ScalaLayouter.scala:52`. Empirical 700-file determinism on the dev host across 2 cold sbt-JVMs is positive; cross-machine determinism not proven. Out of PR-03.1's per-case approval boundary; recorded as F7.

**Fix subagent** (general-purpose, sonnet) applied D11 + D12. Smoke test for D11: corrupted golden → `[error] (idealingua-v1-test-harness / verifyGoldens) izumi.idealingua.harness.GoldenVerificationFailure: Golden verification failed. Mismatched goldens (1 files — run regenerateGoldens to update them): Golden mismatch: scala/idltest/aliases/package-object.scala`; sbt daemon stays alive. Cross-build re-verified on Scala 2.13.18 + 3.8.3.

### Inner loop closeout — single PR-03.1 commit

Orchestrator dropped `Placeholder.scala` (no longer needed; 6 real sources present), wrote the PR-03.1 Completed entry into `tasks.md`, staged the full deliverable (706 new files + 5 modified, excluding `job.md`), and committed at `1b8e1e2`. Post-commit `verifyGoldens` exits 0; tree is clean except for the deliberately-untracked `job.md`.

## Final ledger state

- `./tasks.md`:
  - M2 status: `[~]` (PR-03.1 done; PR-03.2..6 + PR-02 IMPL-1..N still `[ ]`).
  - PR-03.1 marked `[x]`. Rich Completed entry recording verification commands, surprises, and constraints for future PRs.
  - C13 `[x]` (corpus scope = broader 28).
  - F2 status: vacuous on this corpus (zero TBLOB); ready to be marked `[x]` if the user accepts the planner audit.
  - F5 `[ ]` (TBLOB Q3 divergence; urgent for any future TBLOB fixture).
  - F6 `[x]` (ScalaTranslator HashMap-iteration; user-approved deviation).
  - F7 `[ ]` (residual cross-machine determinism risk in TS/C# transpilers; tracked for forward action if CI flakes).

- `./defects.md`: 13 entries (PR-03.1-D01 through PR-03.1-D13), all resolved or deferred. No open `[ ]`. No `[~]` under fix.

- `./defects-m1.md`: closed M1 audit, untouched.

- `./docs/drafts/20260503-2300-PR0301-baseline-harness-impl-plan.md`: planning doc that drove PR-03.1.

- `./docs/drafts/20260503-PR03-backcompat-test-harness-plan.md`: master plan, surgically updated at §11 Q9 (`[x]` resolved) and §12 PR-03.1 wording (corpus path + build entrypoint).

- `./docs/logs/20260504-0113-log.md`: prior session log (R1 escalation).

- `./docs/logs/20260504-1006-log.md`: this log.

- Branch `wip/necromancy`: 6 commits ahead of `origin/wip/necromancy`. Newest 4 commits (this session): `b4556f9` (planning), `bcd0682` (R1 docs fix), `bf68dfa` (T2/T3 ledger + F6 escalation), `1b8e1e2` (PR-03.1 deliverable).

## Stop condition

User's original request — implement PR-03.1 — is fulfilled. PR-03.1 marked `[x]` in tasks.md with a rich Completed entry. PR-03.2 through PR-03.6 and PR-02 IMPL-1..N are scheduled but explicitly out of scope for this session per `job.md`. Returning to the user.

## Pending work for the user

1. **Review and merge PR-03.1**. Branch `wip/necromancy`, commit `1b8e1e2` is the deliverable.
2. **Cut the freeze tag manually post-merge**: `git tag wire-format-baseline-2026-05-03 <merge-commit-sha>`. Per L6 the tag string is locked-immutable; the date does not move.
3. **F5 (TBLOB Q3 divergence)**: before any future PR adds a TBLOB-using fixture, decide between (a) keep the Q3 base64 lock and patch all three emitters in PR-02, (b) downgrade to per-language TBLOB-undefined, (c) other.
4. **F7 (cross-machine determinism residual risk)**: monitor CI / contributor reports; if `verifyGoldens` flakes on a different host, extend the F6-style sort fix to TypeScriptImports / CSharpImports / ServiceRenderer / ScalaLayouter.
5. **PR-02 IMPL-N is still gated on the freeze tag** (per L6 / C1).

## Final report

- Commit SHA: `1b8e1e2cee76cdca9ec1a7d6fc78a1610d893d19` (short: `1b8e1e2`).
- Goldens generated:
  - `scala`: 214 files
  - `typescript`: 257 files
  - `csharp`: 229 files
  - Total: 700 files
- F2 outcome: vacuous (zero TBLOB usage in the in-scope corpus).
- F5 surfaced (TBLOB-emitter divergence beyond C# only).
- F6 resolved (ScalaTranslator non-determinism fixed in-tree, user-approved).
- F7 surfaced (residual cross-machine determinism risk in TS/C# translator iteration patterns).
- No file:line escalation pending.
