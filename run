#!/usr/bin/env bash

set -euo pipefail

if ! command -v mdl >/dev/null 2>&1; then
  echo "mudyla (mdl) is not on PATH. Run via 'nix develop --command mdl ...' or install mudyla."
  exit 1
fi

exec mdl "$@"
